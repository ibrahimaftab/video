export const supportedVideoFormat = ["mp4", "webm", "ogg"];

const html5Video = async (config) => {
  // const { checkVideoBuffer } = await import("./utils.js");
  // checkVideoBuffer(config.selector);
};

export default html5Video